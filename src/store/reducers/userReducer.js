const DEFAULT_STATE = {};

export const userReducer = (state = DEFAULT_STATE, action) => {
    switch (action.type) {
        case "USER_INFO":
            break;
        default:
            break;
    }
    return {...state};
};